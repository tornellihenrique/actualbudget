# Patch inventory

Every commit on `personal` that did not come from upstream gets exactly one row
here. If a patch isn't listed, it will be carried forever by accident — the
inventory is the only place the _reason_ for a patch survives a rebase.

Base: upstream `master` @ `db1b0ea97` (2026-09-08), synced 2026-09-08. The fork
tracks `master` directly, so the base is a commit rather than a tag. Update it on
every rebase — see `fork/RUNBOOK.md`.

## Active patches

| Commit                   | What                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Why                                                                                                                                                                                                                                                                                                                                   | Upstream PR / issue                                                                                                                                        | Drop when                                                                                                                                                                                                                                                                                                               |
| ------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `d94f81aec`              | `sync-server.Dockerfile`: adds `COPY packages/vite-plugin-peggy/package.json` to the deps stage (1 line).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Upstream bug. The deps stage copies workspace manifests one by one and omits `vite-plugin-peggy`, which `packages/api` and `packages/loot-core` depend on via `workspace:*`. Yarn fails resolution before install even starts, so the Railway build dies in the deps stage. **Without this patch the Railway deploy does not build.** | none — not yet sent upstream. Genuine upstream bug, PR-able as-is; re-verified still missing on `master` at `db1b0ea97` (2026-09-08).                      | Upstream adds the `vite-plugin-peggy` manifest COPY (or stops enumerating manifests individually) in `sync-server.Dockerfile`, and we rebase onto a base containing it.                                                                                                                                                 |
| `1d9a807c7`              | `packages/desktop-client/src/browser-preload.js`: appends `REACT_APP_BUILD_METADATA` to the client version as semver build metadata.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | This fork tracks `master`, where `package.json` only changes at release. Every build between two releases reports the same version, so the settings screen can't say which commit an instance is running. Stamping the SHA makes a deploy identifiable.                                                                               | none yet — **PR-able as-is**, deliberately named generically rather than fork-specific, since upstream has the same gap for edge/nightly builds.           | Upstream provides its own way to report a build identity, or this fork goes back to tracking release tags (where the version alone is unambiguous).                                                                                                                                                                     |
| `6249aeb92`              | `packages/sync-server/src/app.ts`: appends `ACTUAL_BUILD_METADATA` to the version reported by `/info`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Server half of the above — the client's settings screen renders `/info`'s version as "Server version", so without this the server stays unidentifiable even when the client is stamped.                                                                                                                                               | none yet — **PR-able as-is**, same reasoning as the client patch.                                                                                          | Same as the client patch; drop both together.                                                                                                                                                                                                                                                                           |
| `c0c0da21c`              | `sync-server.Dockerfile`: takes `ACTUAL_BUILD_METADATA` as a build arg, forwards it to the builder stage as `REACT_APP_BUILD_METADATA` and to the runtime stage as `ACTUAL_BUILD_METADATA`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Deploy plumbing that connects the two patches above to Railway. Without it the build arg never reaches either half.                                                                                                                                                                                                                   | `local-only` — this wiring is specific to how this fork deploys, even if the two code patches land upstream.                                               | The two patches above are dropped, or the deploy stops using this Dockerfile.                                                                                                                                                                                                                                           |
| `0c4c9e86d`              | `sync-server.Dockerfile`: reads `RAILWAY_GIT_COMMIT_SHA` directly — as a build arg for the client, and in the entrypoint for the server — with `ACTUAL_BUILD_METADATA` still taking precedence.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Railway's git variables are deploy-scoped and absent from the service's variable set, so `${{ RAILWAY_GIT_COMMIT_SHA }}` interpolates to an empty string. Without reading it directly, the commit has to be pasted in by hand on every deploy.                                                                                        | `local-only` — extends the wiring above with a Railway-specific fallback, which is exactly why it lives in the Dockerfile and not in the two code patches. | `c0c0da21c` is dropped, or the deploy leaves Railway.                                                                                                                                                                                                                                                                   |
| `da07dec5a`              | Column resizing by dragging header boundaries, plus a right-click header menu (hide, reset width, reset all widths, manage). Routes the previously duplicated widths in `headerConfig` and `renderColumnCell` through one resolved record, adds `width` to the column pref, and gives `StatusCell`/`PayeeCell`/`NotesCell` a width prop. Since 2026-09-08 it also absorbs upstream's amount-column auto-sizing (#8645): `payment`/`deposit`/`balance` default to a new `'auto'` width that `resolveAutoColumnWidths` turns into the measured width, so a column the user has dragged keeps its width and an untouched one still fits its content. That makes upstream's `amountColumnWidths` prop redundant on `TransactionHeader`/`Transaction`/`NewTransaction`, so this patch removes that threading. | Resizing was the point of adopting the column manager early. Uses a redistribute model — the left column's gain is the right column's loss — because the table is a fit-to-width flexbox with no horizontal scroll and a header rendered outside the virtualized body.                                                                | none — **not sent upstream yet, but now PR-able**: it built on PR #8580, which merged upstream as `f72498016`, so the dependency that blocked it is gone.  | Sent upstream and merged. **Header VRT snapshots are stale** — regenerate with `yarn vrt:docker` before this is offered upstream. Higher rebase cost than the other patches: it deletes upstream lines (the `amountColumnWidths` threading) rather than only adding, so re-check `TransactionsTable.tsx` on every sync. |
| `b72369bc3`, `3d88d60d6` | Notes conditions default to `contains` instead of `is`. `b72369bc3`: the blank new-rule form opens on `notes` / `contains` rather than `payee` / `is` (`ManageRules.tsx`, `mobile/rules/MobileRuleEditPage.tsx`). `3d88d60d6`: adds `getDefaultConditionOp` to `util/rule.ts` and routes the three op-defaulting sites through it — the condition `+` button and the field switcher in `RuleEditor.tsx`, and the transaction filter menu in `FiltersMenu.tsx`. Rules seeded from a payee or a transaction are untouched.                                                                                                                                                                                                                                                                                 | Personal workflow. Notes on this instance are always matched as a substring, so the old default meant changing a dropdown before typing on essentially every rule and notes filter.                                                                                                                                                   | `local-only` — a preference, not a fix. Upstream would reasonably want this configurable rather than a changed global default, so it is not PR-able as-is. | Upstream makes the default condition configurable, or the preference changes. **Mobile VRT snapshots are stale** — `Mobile-Rules-clicking-add-button-opens-rule-creation-form-{1,2,3}` screenshot the blank form; regenerate with `yarn vrt:docker`.                                                                    |

## Dropped patches

Patches removed during a rebase, kept for the record so the same change doesn't
get reinvented later.

| Commit                                                                       | What                                                                                                                                                                                               | Dropped on                                  | Reason                                                                                                                                                                                                                                                               |
| ---------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `22d8f948c`                                                                  | `packages/component-library/package.json`: collapsed `test` + `test:web` into one `test` script, dropping the `npm-run-all` wrapper.                                                               | 2026-07-30, syncing onto upstream `master`. | Fixed upstream. `master` carries the identical script, so replaying the patch would have been a no-op. Its inventory row (`f80133c91`) was dropped with it.                                                                                                          |
| `eb1b7f301`, `1472bd974`, `ee7f98025`, `5c51eb130`, `4904f0fda`, `ff5082ed2` | Upstream PR #8580, the transaction-table column manager, vendored ahead of its merge: per-view column visibility and ordering, a manager modal, and the `transaction-table-columns-*` synced pref. | 2026-08-10, syncing onto upstream `master`. | Merged upstream as `f72498016` ("[AI] Add a column manager to the transaction table (#8580)"), exactly the drop condition the row carried. The merged version is byte-identical to what we vendored, so the column resizing patch replayed onto it without conflict. |

## Column meanings

- **Commit** — short SHA on `personal`. Rewritten by every rebase; refresh it
  when you rebase.
- **What** — one line, what the patch actually changes. Name the files if it's
  small enough to say.
- **Why** — the problem it solves for this instance. Not "fixes bug" — what
  broke, and what it looked like.
- **Upstream PR / issue** — link if one exists. `none` if the patch was never
  sent upstream; `local-only` if it's instance-specific and never will be
  (deploy plumbing, personal config) — that distinction decides whether it's
  worth re-checking upstream each release.
- **Drop when** — the concrete condition that retires the patch: an upstream PR
  merging, a release containing the fix, a setting becoming configurable, or a
  local need going away. Every row needs one. If you genuinely can't name a
  condition, write `never (permanent fork divergence)` and mean it.
